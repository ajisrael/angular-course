# Creating Components

Used to allow for DRY principal in HTML templates

## Command

```bash
ng generate component component_name
```

# Migrating to New Control Flow Syntax

Used to move from `*ngFor` syntax to `@for` syntax. (Not just for for loops)

```bash
ng gnerate @angular/core:control-flow
```
